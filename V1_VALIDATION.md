# V1.0 Foundation Validation

- Web-only Next.js project
- TypeScript strict
- Three.js scene
- modular domain structure
- no Apple, Xcode, ARKit or native LiDAR files

A full npm build must be run in an environment with registry access:
```bash
npm install
npm run typecheck
npm run build
```
