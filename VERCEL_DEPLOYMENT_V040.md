# Vercel Deployment V0.40

Alle Dateien dieses vollständigen Pakets müssen direkt in das Hauptverzeichnis des GitHub-Repositorys übernommen werden. `package.json`, `app/` und `components/` müssen auf der ersten Ebene liegen.

Nach dem erfolgreichen Deployment steht oben `V0.40 · PHOTOREAL RENDER CORE`.
