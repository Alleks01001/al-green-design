# Vercel Deployment V0.39

Der Vercel-Screenshot mit Status `Bereit` zeigt, dass die Bereitstellung technisch funktioniert.

Für V0.39 müssen die Dateien aus diesem Paket auf den Branch `main` committed werden. Danach muss ein neues Deployment mit einem neuen Commit-Code erscheinen.

Prüfpunkte auf der veröffentlichten Seite:

- oben steht `V0.39`;
- daneben steht `DETAIL SCENE STUDIO`;
- im Modul `Layer` werden detaillierte Ebenenkarten gezeigt;
- in der 3D-Ansicht sind Gebäude, Pool, Pergola und Pflanzen nicht mehr nur einfache Kästen.
