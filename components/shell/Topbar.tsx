"use client";
import {useEditorStore} from "@/stores/editor-store";
export function Topbar(){const{mode,setMode,view,setView,settings}=useEditorStore();return <header className="topbar"><span className="version-badge">V1.0.4 DEPLOYMENT FIX</span><label>Werkzeug<select value={mode} onChange={e=>setMode(e.target.value as typeof mode)}>{["select","draw","move","rotate","scale","measure"].map(v=><option key={v} value={v}>{v}</option>)}</select></label><label>Ansicht<select value={view} onChange={e=>setView(e.target.value as typeof view)}><option value="2d">2D</option><option value="3d">3D</option><option value="split">Geteilt</option></select></label><span className="status-pill">Raster {settings.gridSize} m</span><span className="status-pill">Fang {settings.snapEnabled?"AN":"AUS"}</span></header>}
